# 忆联 · 身后空间与居所（设计原型 V1.0）

把《忆联_身后空间与居所设计 V1.0》里的设计机制做成可点、可看的前端原型，用于设计验证与内部讨论。

**这不是产品功能，也不是可上线的应用。** 没有后端、没有账号、没有真实存储，数据只活在页面内存里，刷新即回到初始状态。

## 运行

单独跑：

```bash
cd prototype
npm install
npm run dev        # http://localhost:5190
```

随整套系统一起跑：

```bash
./scripts/start-all.sh     # 原型在 http://192.168.32.128:5190
```

## 建议的演示顺序

1. **居所页**：点右上角"屋里"，看外景与客厅剖面两种视角。
2. **放一件**（底部）：依次点"一张照片""一段讲述""一句留给某人的话"，看客厅相框、书架、信箱同时变化——这是"居所随内容变化"的核心机制。
3. **时间**：在"清晨 / 黄昏 / 夜晚"之间切换，看同一座房子的光。
4. **换个地方**：换一个居所。每个地方是"位置 + 房型"的一个完整成品，不做自由拼装。
5. **切"之后"**：右上角"现在 / 之后"开关，看同一间屋子的另一副样子——门口亮灯、堂屋点烛、信件进入待送。
6. **留给某人的话**：看"到那天才打开"的信，以及收信人的"暂不接收"开关。
7. **钥匙**：点"推进一步"，走完 指定开门人 → 申请 → 7 天冷静期 → 开启 的全过程。
8. **一起写**：念念提问 → 爷爷回答 → 这段讲述自动进入书房。
9. **边界**：四层可见性与 8 条设计红线。
10. **门口看一眼**：未登录访客看到的门廊，只暴露门牌级信息。

## 设计文档对应关系

| 原型里的东西 | 设计文档 |
| --- | --- |
| 外景 / 屋里、6 个成套场景 | 4.2 插画场景而非 3D；4.3 成套场景不做组合 |
| 位置是全家一起定的 | 4.1 位置归家族、房间归个人 |
| 相框、书架、亮灯的窗、院里的树 | 4.4 居所随内容变化 |
| 念念提问 → 爷爷回答 | 第三章 三代共写 |
| 信件与开启时间 | 5.1 按时赴约 |
| 收信人"暂不接收" | 5.2 选择性公开的边界要求 |
| 底部"放一件" | 5.3 低门槛 |
| 开门人与 7 天冷静期 | 第八章差距项 / 设计红线 3 |
| 门廊 | 第七章可见性与隐私边界 |
| 边界页的四层与 8 条红线 | 第七章、第十章 |

## 原型没有做的事

- 没有后端、没有真实数据落库、没有登录与权限校验
- 不做 3D 可漫游空间（这是设计决策，不是没来得及做）
- 场景实现了 6 个，设计规划为 8 个成品场景
- 相册里的照片是占位色块，用来演示"墙上多一个相框"，不是真实素材
- 所有人物与事件均为虚构演示数据
